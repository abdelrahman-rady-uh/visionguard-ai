# Cloud deployment requirements — lightweight, no heavy ML models
# The app runs AI analysis using heuristics + HuggingFace Inference API
# Full local model support requires requirements.txt (run locally)

# Core Framework
Flask>=3.0.0
Flask-CORS>=4.0.0
Werkzeug>=3.0.1
gunicorn>=21.2.0
python-dotenv>=1.0.0

# Data Processing (lightweight)
numpy>=2.0.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Security & Encryption
cryptography>=41.0.0

# Authentication
authlib>=1.3.0

# Requests (for HuggingFace Inference API)
requests>=2.31.0

# Database
# SQLAlchemy>=2.0.0

# Logging
python-json-logger>=2.0.0

# Environment & Config
python-decouple>=3.8
